import { createSlice } from "@reduxjs/toolkit";

const initialState = {}

const authenticationSlice = createSlice({
    name:'authentication',
    initialState,
    extraReducers:( builder ) => {
    }
})